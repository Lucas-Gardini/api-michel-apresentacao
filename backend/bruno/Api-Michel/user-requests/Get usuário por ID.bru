meta {
  name: Get usuário por ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/user/{id}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Rota responsável por obter um usuário pelo id, travada a um usuário ADMINISTRADOR.
  
  Autenticação via Bearer Token obtida pela rota de login
}
