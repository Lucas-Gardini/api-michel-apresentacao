meta {
  name: Deletar usuário por ID
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/user/{id}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Rota responsável por excluir um usuário pelo id, travada a um usuário ADMINISTRADOR.
  
  Autenticação via Bearer Token obtida pela rota de login
}
