meta {
  name: Deletar pal por ID
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/pal/{id}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Rota responsável por excluir o pal pelo id.
  
  Autenticação via Bearer Token obtida pela rota de login
}
