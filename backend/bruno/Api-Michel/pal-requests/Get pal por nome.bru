meta {
  name: Get pal por nome
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/pal/{palName}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    
  }
}

docs {
  Rota responsável por buscar um pal pelo nome.
  
  Autenticação via Bearer Token obtida pela rota de login
}
