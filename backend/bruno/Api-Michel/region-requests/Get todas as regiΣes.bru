meta {
  name: Get todas as regiões
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/region
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    
  }
}

docs {
  Rota responsável por obter todas as regiões do palword.
  
  Autenticação via Bearer Token obtida pela rota de login
}
