meta {
  name: Get região por ID
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/region/{regionID}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Rota responsável por obter uma região do palword pelo id.
  
  Autenticação via Bearer Token obtida pela rota de login
}
