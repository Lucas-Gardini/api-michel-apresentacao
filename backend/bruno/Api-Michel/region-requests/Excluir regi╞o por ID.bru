meta {
  name: Excluir região por ID
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/region/{id}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Rota responsável por excluir uma região do palword pelo id.
  
  Autenticação via Bearer Token obtida pela rota de login
}
