meta {
  name: Get aptidão por id
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/aptitude/{aptitudeId}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Rota responsável por obter todas as aptidões do palword pelo id.
  
  Não é possível repetir o email cadastrado.
  Autenticação via Bearer Token obtida pela rota de login
}
