meta {
  name: Criar aptidão para pal
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/aptitude
  body: json
  auth: bearer
}

auth:bearer {
  token: Admin Token
}

body:json {
  {
    "name": "Nome da aptidão",
    "level": 4,
    "pal": { "connect": { "name": "Lamball" } }
  }
}
