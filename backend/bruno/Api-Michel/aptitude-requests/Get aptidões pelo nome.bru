meta {
  name: Get aptidões pelo nome
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/aptitude/name/{aptitudeName}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Rota responsável por obter todas as aptidões do palword pelo nome.
  
  Não é possível repetir o email cadastrado.
  Autenticação via Bearer Token obtida pela rota de login
}
