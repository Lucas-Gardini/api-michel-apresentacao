meta {
  name: Deletar aptidão por id
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/aptitude/delete-pal/{id}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Rota responsável por excluir uma aptidão do palword pelo id.
  
  Não é possível repetir o email cadastrado.
  Autenticação via Bearer Token obtida pela rota de login
}
